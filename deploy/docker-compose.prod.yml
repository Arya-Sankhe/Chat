services:
  klui-chat:
    environment:
      RESEARCH_QUEUE: production
      TRANSCRIBE_QUEUE: production
  research-worker:
    environment:
      RESEARCH_QUEUE: production
  transcriber:
    environment:
      TRANSCRIBE_QUEUE: production
